# RSS-Parsing
feedparser==6.0.11

# HTTP-Requests (für spätere Erweiterungen nützlich)
requests==2.32.3

# Themen-Clustering (Phase 2)
scikit-learn==1.8.0
numpy==2.4.4

# Framing-Analyse (Phase 3) — laeuft ueber die lokale Claude-Code-CLI (`claude`),
# abgerechnet ueber das Monatsabo. Kein API-Key noetig.
# Voraussetzung: Claude Code installiert und angemeldet (siehe README).
# json-repair flickt gelegentlich unescapte Anfuehrungszeichen im Modell-Output.
json-repair>=0.30.0

# API + App (Phase 4)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
