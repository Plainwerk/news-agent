services:
  - type: web
    name: prisma-news
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api:app --host 0.0.0.0 --port $PORT
    # Kein LLM-Key noetig: Die App serviert nur die vorberechnete SQLite-DB.
    # Die Framing-Analyse laeuft lokal ueber das Claude-Abo (siehe README).
